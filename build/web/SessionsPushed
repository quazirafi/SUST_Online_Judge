session.setAttribute("conn", conn);
session.setAttribute("teacher", teacher);
session.setAttribute("tracker", "teacher");
session.setAttribute("courseSession", "2012");ist
session.setAttribute("student", student);
session.setAttribute("tracker", "student");
session.setAttribute("course", course);2nd
session.setAttribute("exam",exmDao.getExamById(examId,conn));3rd
session.setAttribute("exams", exam2);
session.setAttribute("courseTitle", courseTitle);